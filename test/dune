(test
  (name testpass)
  (deps (glob_files ../examples/*.ml) (glob_files runpass/*.ml))
  (modules testpass)
  (libraries milner llvm.analysis unix))

(test
  (name testllvm)
  (modules testllvm)
  (libraries milner llvm llvm.analysis))
