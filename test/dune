(test
  (name testpass)
  (deps (glob_files runpass/*.ml))
  (modules testpass)
  (libraries milner llvm.analysis))

(test
  (name testllvm)
  (modules testllvm)
  (libraries milner llvm llvm.analysis))
